spring:
  application:
    name: Ordens de Servico API
  datasource:
    url: jdbc:h2:mem:os
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /h2-console